name: project_hermes_v1
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  gql: ^0.12.4
  gql_exec: ^0.2.5
  gql_http_link: ^0.3.2
  gql_link: ^0.3.1
  http: ^0.12.1
  oauth2: ^1.6.3
  url_launcher: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.1
  gql_build: ^0.0.11
  pedantic: ^1.9.0

flutter:
  uses-material-design: true
